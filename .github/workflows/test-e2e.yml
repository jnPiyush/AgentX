# End-to-End Tests for Multi-Agent Orchestration
# Tests full workflow: Epic → PM → Architect → Engineer → Reviewer
# Manual trigger: gh workflow run test-e2e.yml
# Scheduled: Daily at 2 AM UTC

name: E2E Tests - Workflow Orchestration

on:
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Test suite to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - smoke
          - orchestration
          - triggers
          - metrics
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  pull_request:
    paths:
      - '.github/workflows/**'
      - '.github/agents/**'

# Grant permissions for issue creation and workflow triggering
permissions:
  contents: read
  issues: write
  actions: write

# Prevent concurrent test runs
concurrency:
  group: e2e-tests-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  # ==========================================================================
  # SMOKE TESTS - Quick validation of core functionality
  # ==========================================================================
  smoke-tests:
    name: Smoke Tests
    runs-on: ubuntu-latest
    if: inputs.test_suite == 'smoke' || inputs.test_suite == 'all' || github.event_name == 'schedule'
    env:
      GH_TOKEN: ${{ github.token }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test 1 - Workflow files are valid
        run: |
          echo "🧪 Testing: All workflow files are valid YAML"
          
          for workflow in .github/workflows/*.yml; do
            echo "Validating: $workflow"
            gh workflow view $(basename $workflow) || exit 1
          done
          
          echo "✅ All workflow files valid"

      - name: Test 2 - Agent files have required frontmatter
        run: |
          echo "🧪 Testing: Agent files have required YAML frontmatter"
          
          for agent in .github/agents/*.agent.md; do
            echo "Checking: $agent"
            
            # Check for required description field
            if ! grep -q "description:" "$agent"; then
              echo "❌ Missing 'description' in $agent"
              exit 1
            fi
            
            # Check for tools field
            if ! grep -q "tools:" "$agent"; then
              echo "⚠️ Warning: Missing 'tools' in $agent"
            fi
          done
          
          echo "✅ All agent files have required frontmatter"

      - name: Test 3 - Skills have required frontmatter
        run: |
          echo "🧪 Testing: Skill files follow agentskills.io spec"
          
          for skill in skills/*.md; do
            echo "Checking: $skill"
            
            # Check for YAML frontmatter
            if ! head -n 1 "$skill" | grep -q "^---$"; then
              echo "⚠️ Warning: Missing YAML frontmatter in $skill"
            fi
          done
          
          echo "✅ Skills frontmatter check complete"

      - name: Test 4 - Security config exists
        run: |
          echo "🧪 Testing: Security configuration files exist"
          
          # Check autonomous-mode.yml
          if [ ! -f .github/autonomous-mode.yml ]; then
            echo "❌ Missing .github/autonomous-mode.yml"
            exit 1
          fi
          
          # Check CODEOWNERS
          if [ ! -f .github/CODEOWNERS ]; then
            echo "❌ Missing .github/CODEOWNERS"
            exit 1
          fi
          
          # Validate autonomous-mode.yml has required keys
          if ! grep -q "autonomous:" .github/autonomous-mode.yml; then
            echo "❌ Missing 'autonomous' key in autonomous-mode.yml"
            exit 1
          fi
          
          echo "✅ Security configuration valid"

      - name: Test 5 - Documentation completeness
        run: |
          echo "🧪 Testing: Core documentation exists"
          
          required_docs=(
            "README.md"
            "AGENTS.md"
            "Skills.md"
            "docs/technical-specification.md"
            "docs/mcp-integration.md"
          )
          
          for doc in "${required_docs[@]}"; do
            if [ ! -f "$doc" ]; then
              echo "❌ Missing: $doc"
              exit 1
            fi
            echo "✓ Found: $doc"
          done
          
          echo "✅ All required documentation exists"

  # ==========================================================================
  # ORCHESTRATION TESTS - Full workflow integration
  # ==========================================================================
  test-orchestration-flow:
    name: Test Orchestration Flow
    runs-on: ubuntu-latest
    if: inputs.test_suite == 'orchestration' || inputs.test_suite == 'all'
    env:
      GH_TOKEN: ${{ github.token }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test - Validate orchestration workflow exists
        run: |
          echo "🧪 Testing: Orchestration workflow is configured correctly"
          
          # Check orchestrate.yml exists
          if [ ! -f ".github/workflows/orchestrate.yml" ]; then
            echo "❌ orchestrate.yml not found"
            exit 1
          fi
          
          # Validate workflow syntax
          gh workflow view orchestrate.yml
          
          echo "✅ Orchestration workflow validated"

      - name: Test - Verify orchestration logic
        run: |
          echo "🧪 Testing: Orchestration workflow has correct triggers"
          
          # Check for issues trigger
          if ! grep -q "issues:" ".github/workflows/orchestrate.yml"; then
            echo "❌ Missing issues trigger"
            exit 1
          fi
          
          # Check for workflow_dispatch
          if ! grep -q "workflow_dispatch:" ".github/workflows/orchestrate.yml"; then
            echo "❌ Missing workflow_dispatch trigger"
            exit 1
          fi
          
          echo "✅ Orchestration triggers validated"

      - name: Test - Verify agent routing logic exists
        run: |
          echo "🧪 Testing: Orchestration workflow routes to correct agents"
          
          # Check for PM routing
          if ! grep -q "run-product-manager.yml" ".github/workflows/orchestrate.yml"; then
            echo "❌ Missing PM routing"
            exit 1
          fi
          
          # Check for Architect routing
          if ! grep -q "architect.yml" ".github/workflows/orchestrate.yml"; then
            echo "❌ Missing Architect routing"
            exit 1
          fi
          
          echo "✅ Agent routing logic validated"

  # ==========================================================================
  # TRIGGER TESTS - Event-driven architecture validation
  # ==========================================================================
  test-event-driven-triggers:
    name: Test Event-Driven Triggers
    runs-on: ubuntu-latest
    if: inputs.test_suite == 'triggers' || inputs.test_suite == 'all'
    env:
      GH_TOKEN: ${{ github.token }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test - Workflow dispatch with gh CLI
        run: |
          echo "🧪 Testing: Direct workflow triggering with gh CLI"
          
          # Test triggering orchestrator
          gh workflow run orchestrate.yml -f issue_number="999" || {
            echo "⚠️ Trigger failed (expected if issue doesn't exist)"
          }
          
          echo "✅ Workflow dispatch mechanism works"

      - name: Test - Verify workflow accepts inputs
        run: |
          echo "🧪 Testing: Workflows accept issue_number input"
          
          for workflow in architect.yml engineer.yml reviewer.yml ux-designer.yml; do
            echo "Checking: $workflow"
            
            # Check for workflow_dispatch
            if ! grep -q "workflow_dispatch:" ".github/workflows/$workflow"; then
              echo "❌ $workflow missing workflow_dispatch trigger"
              exit 1
            fi
            
            # Check for issue_number input
            if ! grep -q "issue_number:" ".github/workflows/$workflow"; then
              echo "❌ $workflow missing issue_number input"
              exit 1
            fi
            
            echo "✓ $workflow has correct trigger configuration"
          done
          
          echo "✅ All workflows have workflow_dispatch with required inputs"

      - name: Test - Verify gh workflow run commands in workflows
        run: |
          echo "🧪 Testing: Workflows contain gh workflow run commands"
          
          # Check engineer.yml triggers reviewer
          if ! grep -q "gh workflow run reviewer.yml" .github/workflows/engineer.yml; then
            echo "❌ engineer.yml doesn't trigger reviewer.yml"
            exit 1
          fi
          
          # Check architect.yml triggers engineer
          if ! grep -q "gh workflow run engineer.yml" .github/workflows/architect.yml; then
            echo "❌ architect.yml doesn't trigger engineer.yml"
            exit 1
          fi
          
          echo "✅ Event-driven triggers implemented correctly"

  # ==========================================================================
  # METRICS TESTS - Observability validation
  # ==========================================================================
  test-metrics-collection:
    name: Test Metrics Collection
    runs-on: ubuntu-latest
    if: inputs.test_suite == 'metrics' || inputs.test_suite == 'all'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test - Workflows record metrics
        run: |
          echo "🧪 Testing: Workflows have metrics recording steps"
          
          workflows=("engineer.yml" "architect.yml" "run-product-manager.yml")
          
          for workflow in "${workflows[@]}"; do
            echo "Checking: $workflow"
            
            # Check for metrics recording step
            if ! grep -q "Record workflow metrics" ".github/workflows/$workflow"; then
              echo "❌ $workflow missing metrics recording"
              exit 1
            fi
            
            # Check for workflow_duration output
            if ! grep -q "workflow_duration" ".github/workflows/$workflow"; then
              echo "❌ $workflow missing workflow_duration metric"
              exit 1
            fi
            
            # Check for timestamp output
            if ! grep -q "timestamp" ".github/workflows/$workflow"; then
              echo "❌ $workflow missing timestamp metric"
              exit 1
            fi
            
            echo "✓ $workflow has metrics collection"
          done
          
          echo "✅ All workflows collect metrics"

      - name: Test - Metrics use GITHUB_OUTPUT
        run: |
          echo "🧪 Testing: Metrics use GITHUB_OUTPUT for data passing"
          
          if ! grep -q ">> \$GITHUB_OUTPUT" .github/workflows/engineer.yml; then
            echo "❌ engineer.yml not using GITHUB_OUTPUT"
            exit 1
          fi
          
          echo "✅ Workflows use GITHUB_OUTPUT correctly"

      - name: Test - Metrics logged to console
        run: |
          echo "🧪 Testing: Metrics are logged for visibility"
          
          # Check for metrics logging
          if ! grep -q "Workflow Metrics" .github/workflows/engineer.yml; then
            echo "⚠️ Warning: engineer.yml may not log metrics"
          fi
          
          echo "✅ Metrics logging validated"

  # ==========================================================================
  # SUMMARY - Report results
  # ==========================================================================
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [smoke-tests, test-orchestration-flow, test-event-driven-triggers, test-metrics-collection]
    if: always()
    
    steps:
      - name: Generate test summary
        uses: actions/github-script@v7
        with:
          script: |
            const results = {
              smoke: '${{ needs.smoke-tests.result }}',
              orchestration: '${{ needs.test-orchestration-flow.result }}',
              triggers: '${{ needs.test-event-driven-triggers.result }}',
              metrics: '${{ needs.test-metrics-collection.result }}'
            };
            
            console.log('📊 E2E Test Results:');
            console.log('====================');
            
            let allPassed = true;
            for (const [test, result] of Object.entries(results)) {
              const icon = result === 'success' ? '✅' : result === 'skipped' ? '⏭️' : '❌';
              console.log(`${icon} ${test}: ${result}`);
              if (result === 'failure') allPassed = false;
            }
            
            console.log('====================');
            
            if (allPassed) {
              console.log('✅ All E2E tests passed!');
            } else {
              console.log('❌ Some E2E tests failed');
              core.setFailed('E2E test suite failed');
            }
            
            // Create summary
            await core.summary
              .addHeading('E2E Test Results')
              .addTable([
                [{data: 'Test Suite', header: true}, {data: 'Result', header: true}],
                ['Smoke Tests', results.smoke],
                ['Orchestration Flow', results.orchestration],
                ['Event-Driven Triggers', results.triggers],
                ['Metrics Collection', results.metrics]
              ])
              .write();
